[project]
name = "evaluation"
version = "1.0.0"
description = "Python project to evaluate different resource-friendly LLMs in Geman Easy Language capabilities"
license = { text = "MIT" }
authors = [
    { name = "Tobias Stadler" }
]
repository = "https://github.com/devtobi/aigelb"
readme = "README.md"
keywords = [ "llm", "evaluation", "metrics", "nlp", "mt", "easylanguage" ]
requires-python = "==3.13.3"
dependencies = [
    "dotenv (==0.9.9)",
    "huggingface-hub[hf-xet] (==0.32.3)",
    "transformers (==4.52.4)",
    "torch (==2.7.0)",
    "llama-cpp-python (==0.3.9)",
]

[dependency-groups]
dev = [
    "ruff (==0.11.12)",
]

[tool.uv]
package = false
